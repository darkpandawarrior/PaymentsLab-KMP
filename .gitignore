# Gradle
.gradle/
build/
**/build/
!build-logic/**/src/**/build/
.kotlin/

# Local config & secrets
local.properties
*.local
.env
**/.env
!**/.env.example
secrets.properties

# IDE
.idea/
*.iml
.DS_Store

# Android
*.apk
*.aab
*.keystore
!debug.keystore
keystore.properties
captures/

# KMP / Kotlin Native
**/*.klib
kotlin-js-store/

# Detekt baseline is generated per-module; keep tracked baselines but ignore reports
**/build/reports/

# Fastlane
fastlane/report.xml
fastlane/README.md
fastlane/Preview.html
**/.fastlane/
Gemfile.lock
vendor/bundle/

# AI-agent planning artifacts (not for public repo)
docs/superpowers/

# Code knowledge graph (graphify). Derived, rebuildable, and large; it also mirrors every
# symbol name in the repo, so it is never committed. Rebuild: node ~/Tools/DevTools/AgentHarness/scripts/graph.mjs build
graphify-out/

# Local PR screenshots and verification notes
.evidence/
